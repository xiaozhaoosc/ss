---
name: 同事
description: 一个具有高度共情力、主动性和系统思维的协作型 AI 伙伴，融合了“人到 Skill”的萃取框架。
---

# 同事 (Colleague) Skill Protocol & 萃取框架

## 1. 知识萃取：从“人”到“Skill”的蒸馏
（注：此部分根据你的输入还原，旨在将隐性知识显性化）

### 第一步：把隐性知识“挤”出来
人脑中的东西往往是模糊、情境化的，AI 吃不进去。需要先用对话和记录把它显性化。

**执行动作：**
1. **“复盘式访谈”**：
   找一个典型任务（如“处理 SQL 报错”、“设计 Parent Center”），边做边思考并记录。追问：“为什么选 A 而不是 B？”
2. **收集“决策快照”**：
   摘录过往记录中带着判断的话。例如：“`create_dept` 必须是公共字段，否则多租户权限会穿透。”
3. **结构化“场景-判断-行动”卡片**：
   将信息转化为 AI 可理解的逻辑。

---

## 2. 小步项目协作准则 (Small Steps Project)

### 2.1 影子审计 (Shadow Audit) - 针对 create_dept 专项
- **强制检查**：所有实体类必须继承 `BaseEntity`。
- **数据库对齐**：所有 `ss_` 开头的表必须具备 `create_dept`, `create_by`, `create_time`, `update_by`, `update_time`, `del_flag`。

### 2.2 UI/UX 守护
- **硬件端**：死守 128x160 阵地。
- **家长端**：追求极致的呼吸感与微动效。

## 3. 当前任务：0503_todo 闭环
1. **[P0] 修复 SQL 报错**：执行 `patch_base_entity_fields_20260508.sql`。
2. **[P1] 家长中心模块化**：通知、隐私、安全、反馈。
