# Small Steps 自动化测试方案

## 1. 测试策略概述

### 1.1 测试目标
- 确保各项目功能正常运行
- 提高代码质量和稳定性
- 减少回归错误
- 支持持续集成和持续部署

### 1.2 测试层次
- **单元测试**：测试单个组件或函数
- **集成测试**：测试组件间的交互
- **端到端测试**：测试完整的业务流程
- **性能测试**：测试系统性能和响应时间

## 2. 各项目测试方案

### 2.1 smallsteps-api (Java Spring Boot)

#### 2.1.1 测试框架
- **单元测试**：JUnit 5 + Mockito
- **集成测试**：Spring Boot Test
- **API测试**：RestAssured
- **性能测试**：JMeter

#### 2.1.2 测试覆盖范围
- 控制器层 (Controllers)
- 服务层 (Services)
- 数据访问层 (Repositories)
- 工具类 (Utils)
- API接口

#### 2.1.3 测试目录结构
```
smallsteps-api/
├── smallsteps-modules/
│   ├── smallsteps-system/
│   │   └── src/test/
│   ├── smallsteps-child/
│   │   └── src/test/
│   └── smallsteps-parent/
│       └── src/test/
└── smallsteps-common/
    └── smallsteps-common-core/
        └── src/test/
```

### 2.2 smallsteps-ui (Vue 3 + TypeScript)

#### 2.2.1 测试框架
- **单元测试**：Vitest + Vue Test Utils
- **端到端测试**：Playwright
- **组件测试**：Vitest + Vue Test Utils

#### 2.2.2 测试覆盖范围
- 组件 (Components)
- 页面 (Pages)
- 工具函数 (Utils)
- 路由 (Router)
- 状态管理 (Store)

#### 2.2.3 测试目录结构
```
smallsteps-ui/
└── src/
    ├── components/
    │   └── __tests__/
    ├── views/
    │   └── __tests__/
    ├── utils/
    │   └── __tests__/
    └── store/
        └── __tests__/
```

### 2.3 smallsteps-app (UniApp X)

#### 2.3.1 测试框架
- **单元测试**：Vitest
- **端到端测试**：UniApp Automator
- **组件测试**：Vitest + Vue Test Utils

#### 2.3.2 测试覆盖范围
- 页面 (Pages)
- 组件 (Components)
- 工具函数 (Utils)
- API 调用
- 跨端兼容性

#### 2.3.3 测试目录结构
```
smallsteps-app/
└── src/
    ├── pages/
    │   └── __tests__/
    ├── components/
    │   └── __tests__/
    └── utils/
        └── __tests__/
```

### 2.4 smallsteps-esp32 (MicroPython)

#### 2.4.1 测试框架
- **单元测试**：pytest-micropython
- **集成测试**：自定义测试脚本
- **硬件测试**：实物测试

#### 2.4.2 测试覆盖范围
- 硬件驱动
- 网络连接
- MQTT 通信
- 任务管理
- 配置管理

#### 2.4.3 测试目录结构
```
smallsteps-esp32/
└── tests/
    ├── test_hw.py
    ├── test_mqtt.py
    ├── test_config.py
    └── test_task.py
```

## 3. 测试工具和框架选择

| 项目 | 测试类型 | 工具/框架 | 版本 |
|------|---------|-----------|------|
| smallsteps-api | 单元测试 | JUnit 5 | 5.10.0+ |
| smallsteps-api | 集成测试 | Spring Boot Test | 3.5.9 |
| smallsteps-api | API测试 | RestAssured | 5.4.0+ |
| smallsteps-ui | 单元测试 | Vitest | 3.2.4 |
| smallsteps-ui | 端到端测试 | Playwright | 1.40.0+ |
| smallsteps-app | 单元测试 | Vitest | 1.2.2 |
| smallsteps-app | 端到端测试 | UniApp Automator | 3.0.0+ |
| smallsteps-esp32 | 单元测试 | pytest-micropython | 0.3.0+ |

## 4. 测试用例设计

### 4.1 测试用例模板

#### 4.1.1 单元测试用例模板
```
# 测试用例：[功能名称]

## 测试目的
验证 [功能名称] 的正确性

## 测试环境
- 环境：[开发/测试]
- 工具：[测试框架]

## 测试步骤
1. 准备测试数据
2. 执行测试方法
3. 验证测试结果

## 预期结果
- [预期结果1]
- [预期结果2]

## 实际结果
- [实际结果1]
- [实际结果2]

## 测试状态
- [通过/失败]
```

#### 4.1.2 端到端测试用例模板
```
# 测试用例：[业务流程名称]

## 测试目的
验证 [业务流程名称] 的完整流程

## 测试环境
- 环境：[开发/测试]
- 工具：[测试框架]

## 测试步骤
1. 打开应用/页面
2. 执行操作1
3. 执行操作2
4. ...

## 预期结果
- [预期结果1]
- [预期结果2]

## 实际结果
- [实际结果1]
- [实际结果2]

## 测试状态
- [通过/失败]
```

### 4.2 示例测试用例

#### 4.2.1 smallsteps-api 示例
```java
// 用户登录测试
@Test
void testUserLogin() {
    // 准备测试数据
    LoginRequest request = new LoginRequest();
    request.setUsername("admin");
    request.setPassword("123456");
    
    // 执行测试
    LoginResponse response = userService.login(request);
    
    // 验证结果
    assertNotNull(response);
    assertNotNull(response.getToken());
    assertEquals(200, response.getCode());
}
```

#### 4.2.2 smallsteps-ui 示例
```javascript
// 登录页面测试
import { mount } from '@vue/test-utils'
import LoginPage from '@/views/login.vue'

describe('LoginPage', () => {
  test('should render login form', () => {
    const wrapper = mount(LoginPage)
    expect(wrapper.find('form').exists()).toBe(true)
    expect(wrapper.find('input[type="username"]').exists()).toBe(true)
    expect(wrapper.find('input[type="password"]').exists()).toBe(true)
  })
  
  test('should submit form with valid credentials', async () => {
    const wrapper = mount(LoginPage)
    await wrapper.find('input[type="username"]').setValue('admin')
    await wrapper.find('input[type="password"]').setValue('123456')
    await wrapper.find('button[type="submit"]').trigger('click')
    // 验证提交逻辑
  })
})
```

## 5. CI/CD 集成方案

### 5.1 GitHub Actions 配置

#### 5.1.1 smallsteps-api CI 配置
```yaml
name: SmallSteps API CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up JDK 21
      uses: actions/setup-java@v3
      with:
        java-version: '21'
        distribution: 'adopt'
    
    - name: Build with Maven
      run: mvn -B package --file smallsteps-api/pom.xml
    
    - name: Run tests
      run: mvn -B test --file smallsteps-api/pom.xml
```

#### 5.1.2 smallsteps-ui CI 配置
```yaml
name: SmallSteps UI CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
    
    - name: Install dependencies
      run: npm install
      working-directory: smallsteps-ui
    
    - name: Run tests
      run: npm test
      working-directory: smallsteps-ui
    
    - name: Build
      run: npm run build:prod
      working-directory: smallsteps-ui
```

### 5.2 测试覆盖率报告
- **smallsteps-api**：使用 JaCoCo 生成测试覆盖率报告
- **smallsteps-ui**：使用 Vitest 内置的覆盖率报告
- **smallsteps-app**：使用 Vitest 内置的覆盖率报告

## 6. 测试执行流程

### 6.1 本地测试流程
1. 开发人员编写代码
2. 运行单元测试：
   - smallsteps-api: `mvn test`
   - smallsteps-ui: `npm test`
   - smallsteps-app: `npm test`
   - smallsteps-esp32: `pytest tests/`
3. 提交代码前确保所有测试通过

### 6.2 CI/CD 测试流程
1. 代码推送至远程仓库
2. CI 系统自动执行测试
3. 生成测试报告和覆盖率报告
4. 测试通过后部署至测试环境
5. 手动执行端到端测试
6. 测试通过后部署至生产环境

## 7. 测试报告和监控

### 7.1 测试报告
- **单元测试报告**：每次 CI 执行后生成
- **端到端测试报告**：每次发布前生成
- **覆盖率报告**：每周生成一次

### 7.2 测试监控
- 监控测试通过率
- 监控测试覆盖率
- 监控测试执行时间
- 及时发现和解决测试失败问题

## 8. 测试规范和最佳实践

### 8.1 测试规范
- 测试用例应该具有独立性
- 测试用例应该覆盖正面和负面场景
- 测试用例应该易于理解和维护
- 测试代码应该遵循与生产代码相同的代码规范

### 8.2 最佳实践
- 采用 Test-Driven Development (TDD) 方法
- 定期运行测试以确保代码质量
- 及时更新测试用例以适应代码变更
- 优先测试核心功能和高频使用场景

## 9. 结论

本自动化测试方案旨在为 Small Steps 项目提供全面的测试覆盖，确保系统的稳定性和可靠性。通过合理的测试策略和工具选择，可以有效提高代码质量，减少错误，加速开发和部署流程。

各项目团队应该严格按照本方案执行测试，确保测试覆盖达到预期目标，为用户提供高质量的产品。