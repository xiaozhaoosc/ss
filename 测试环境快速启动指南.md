# Small Steps 测试环境快速启动指南

## 前置检查清单

在开始之前，请确保你的开发环境满足以下要求：

- [ ] 操作系统：Windows 10+/macOS 10.15+/Ubuntu 20.04+
- [ ] Git：已安装并配置
- [ ] Docker：20.10+
- [ ] Docker Compose：2.0+
- [ ] JDK：21
- [ ] Maven：3.8+
- [ ] Node.js：18+
- [ ] npm：9+

---

## 第一步：获取代码

```bash
# 克隆代码仓库
git clone <your-repository-url>
cd smallsteps

# 查看当前目录结构
ls -la
```

---

## 第二步：启动基础设施服务（Docker）

我们使用 Docker Compose 来启动数据库、Redis 等基础服务。

### 2.1 进入 Docker 配置目录

```bash
cd docs/dockers
```

### 2.2 检查环境变量配置

查看 `.env` 文件（如果不存在，可以复制示例）：

```bash
# 查看 .env 配置
cat .env

# 如果没有 .env，检查是否有示例文件
ls -la
```

### 2.3 启动服务

```bash
# 启动所有服务（后台运行）
docker-compose up -d

# 查看服务状态
docker-compose ps

# 查看服务日志（可选）
docker-compose logs -f
```

**预期输出**：
- PostgreSQL 容器状态为 `Up`
- Redis 容器状态为 `Up`
- 其他依赖服务状态正常

### 2.4 验证数据库连接

```bash
# 进入数据库容器
docker exec -it smallsteps-postgres psql -U postgres -d smallsteps

# 查看数据库表（在 psql 中）
\dt

# 退出 psql
\q
```

---

## 第三步：初始化数据库

### 3.1 执行数据库初始化脚本

```bash
# 回到项目根目录
cd ../../

# 查看 SQL 脚本目录
ls -la docs/sqls/

# 根据实际情况执行初始化脚本
# 通常使用 init_smallsteps.sql 或其他初始化脚本
```

### 3.2 导入测试数据（可选）

如果需要测试数据，可以导入测试数据脚本：

```bash
# 导入测试数据
docker exec -i smallsteps-postgres psql -U postgres -d smallsteps < docs/sqls/test_data_smallsteps.sql
```

### 3.3 验证数据

```bash
# 再次进入数据库检查数据
docker exec -it smallsteps-postgres psql -U postgres -d smallsteps

# 查看用户表数据
SELECT * FROM sys_user;

# 退出
\q
```

---

## 第四步：启动后端服务

### 4.1 进入后端目录

```bash
cd smallsteps-api
```

### 4.2 使用 Maven 编译项目

```bash
# 清理并编译
mvn clean install -DskipTests

# 如果编译失败，检查 JDK 版本
java -version
mvn -version
```

### 4.3 配置数据库连接（如需要）

检查配置文件：

```bash
# 查看配置文件
ls -la smallsteps-admin/src/main/resources/

# 根据需要编辑 application.yml 或 application-dev.yml
```

### 4.4 启动后端服务

```bash
# 进入 admin 模块
cd smallsteps-admin

# 启动 Spring Boot 应用
mvn spring-boot:run
```

**预期输出**：
- Spring Boot 启动成功
- 端口通常为 8080
- 无错误日志

### 4.5 验证后端服务

打开新的终端窗口：

```bash
# 测试健康检查接口
curl http://localhost:8080/actuator/health

# 或者测试登录接口
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"123456"}'
```

---

## 第五步：启动前端服务（管理后台）

### 5.1 打开新的终端窗口

### 5.2 进入前端目录

```bash
cd smallsteps-ui
```

### 5.3 安装依赖

```bash
# 安装 npm 依赖
npm install

# 如果速度慢，可以使用国内镜像
npm install --registry=https://registry.npmmirror.com
```

### 5.4 配置 API 地址（如需要）

检查环境变量配置：

```bash
# 查看 .env 文件
cat .env.development
```

### 5.5 启动开发服务器

```bash
npm run dev
```

**预期输出**：
- 开发服务器启动成功
- 显示访问地址，通常是 `http://localhost:3000` 或 `http://localhost:5173`

### 5.6 访问管理后台

打开浏览器访问：`http://localhost:5173`（或实际显示的地址）

使用测试账号登录：
- 用户名：`admin`
- 密码：`123456`

---

## 第六步：启动移动应用（可选）

### 6.1 打开新的终端窗口

### 6.2 进入移动应用目录

```bash
cd smallsteps-app
```

### 6.3 安装依赖

```bash
npm install
```

### 6.4 配置 API 地址

检查配置文件中的 API 地址设置。

### 6.5 启动应用

```bash
npm run dev
```

### 6.6 访问应用

在浏览器或移动设备模拟器中访问显示的地址。

---

## 第七步：验证整个系统

### 7.1 功能测试清单

- [ ] 访问管理后台并登录成功
- [ ] 访问家长端并登录成功
- [ ] 访问儿童端并登录成功
- [ ] 创建一个测试任务
- [ ] 执行任务并获得积分
- [ ] 兑换奖励
- [ ] 查看数据统计

### 7.2 测试账号

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 管理员 | admin | 123456 | 管理后台 |
| 家长 | parent | 123456 | 家长端 |
| 儿童 | child | 123456 | 儿童端 |

---

## 常见问题解决

### 问题 1：Docker 服务无法启动

**解决方案**：
```bash
# 检查 Docker 服务状态
docker info

# 如果 Docker 没有运行，启动 Docker
# Windows/Mac：打开 Docker Desktop
# Linux：
sudo systemctl start docker
```

### 问题 2：端口被占用

**解决方案**：
```bash
# 查看端口占用情况
# Windows
netstat -ano | findstr :8080

# macOS/Linux
lsof -i :8080

# 修改配置文件中的端口号，或者关闭占用端口的程序
```

### 问题 3：Maven 依赖下载失败

**解决方案**：
```bash
# 配置 Maven 镜像源
# 编辑 ~/.m2/settings.xml，添加阿里云镜像

# 或者使用命令行参数
mvn clean install -Dmaven.repo.local=./.m2/repository
```

### 问题 4：npm install 失败

**解决方案**：
```bash
# 清理 npm 缓存
npm cache clean --force

# 使用国内镜像
npm install --registry=https://registry.npmmirror.com

# 删除 node_modules 和 package-lock.json，重新安装
rm -rf node_modules package-lock.json
npm install
```

### 问题 5：数据库连接失败

**解决方案**：
- 检查 Docker 容器是否正常运行：`docker-compose ps`
- 检查数据库配置文件中的连接信息
- 确认数据库用户名、密码、数据库名是否正确

---

## 停止环境

当测试完成后，可以按以下步骤停止环境：

### 停止前端服务

在运行前端的终端按 `Ctrl + C`

### 停止后端服务

在运行后端的终端按 `Ctrl + C`

### 停止 Docker 服务

```bash
cd docs/dockers

# 停止服务但保留数据
docker-compose stop

# 或者完全停止并删除容器（数据会保留在卷中）
docker-compose down

# 如果需要完全清理（包括数据卷，谨慎操作）
docker-compose down -v
```

---

## 快速重启

如果需要重启整个环境：

```bash
# 1. 停止所有服务（按 Ctrl+C 停止前端和后端）

# 2. 重启 Docker 服务
cd docs/dockers
docker-compose restart

# 3. 等待 Docker 服务就绪

# 4. 启动后端
cd ../../smallsteps-api/smallsteps-admin
mvn spring-boot:run

# 5. 在新终端启动前端
cd ../../smallsteps-ui
npm run dev
```

---

## 下一步

环境搭建完成后，请参考：
- [自测方案.md](./自测方案.md) - 进行系统自测
- [测试用例详细版.md](./测试用例详细版.md) - 执行详细测试用例
- [requirements/TEST_PLAN.md](./requirements/TEST_PLAN.md) - 完整测试计划

---

**文档版本**：v1.0  
**最后更新**：2026-04-23
