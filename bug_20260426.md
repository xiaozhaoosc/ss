# Small Steps Bug Report (2026-04-26)

> [!NOTE]
> 本报告由 Antigravity 自动化扫描生成并更新。

## 待修复 Bug (Open Issues)

### 1. 儿童端: AI 流式对话 404
- **位置**: `http://localhost:9090/#/pages/child/treehole-chat/index`
- **现象**: 发送消息时，请求 `GET /ssapi/child/ai/chat/stream` 返回 404。
- **调查**: 后端 `ChildAIController` 已定义该路径，但 DispatcherServlet 无法匹配。非流式接口 `POST /ssapi/child/ai/chat` 正常。
- **状态**: 🔴 待修复。

## 已修复 Bug (Resolved Issues)

### 1. 全局: 刷新页面导致状态丢失 (Vuex/Pinia Persistence)
- **现象**: 刷新页面后，用户身份丢失，顶部 Bar 显示“未登录”。
- **修复**: 在 `App.vue` 中增加了 `onLaunch` 恢复身份逻辑。
- **验证**: 家长/儿童账户刷新后均能正确恢复姓名。

### 2. 儿童端: 无法访问自己的成就/任务数据 (Unauthorized)
- **现象**: 儿童登录后首页报 500，“无权访问该儿童数据”。
- **修复**: 修正了后端 `validateChildAccess` 逻辑，允许儿童本人访问。
- **验证**: `child_xiaoming` 首页数据加载正常。

---

## 移动端 (9090) - 家长端

### 1. 核心文案/功能缺失: 时光机 (Time Machine)
- **现象**: 在家长仪表盘 (`/pages/parent/dashboard/index`) 中，未发现“时光机”入口或文案。
- **预期**: 根据 PRD，仪表盘应包含“时光机”功能入口，用于查看孩子的情绪/成长信件。
- **状态**: 🔴 缺失。
- **截图/证据**: `Playwright` 自动化测试 `Check Dashboard Metrics` 失败。

### 2. 交互逻辑: “新增任务”按钮难以定位
- **现象**: Playwright 默认选择器 (`text=新增`, `text=添加`) 无法直接定位到新增任务按钮。
- **建议**: 在 Uni-app 源码中为浮动按钮 (FAB) 增加显式的 `id="btn-add-task"` 或 `data-testid`。

---

## 管理端 (88)

### 1. 登录会话过期提示  问题忽略，重新登录就可以
- **现象**: 首次进入登录页时，直接弹出了“无效的会话，或者会话已过期，请重新登录。”
- **状态**: 🟡 体验问题。
- **建议**: 仅在用户手动退出或 Token 校验失败时显示，不应在初次加载登录页时显示。
