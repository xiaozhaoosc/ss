# 2026-04-27 自动化测试Bug记录 (Playwright)

## 测试目标
测试“家长中心”和“洞察”菜单中的功能、页面、子页面及按钮。
测试环境：`http://localhost:9090/`
测试账号：ken2zhao, parent_zhang, child_xiaoming, child_xiaohong

## 致命错误 (Blocker)
**所有账号登录流程全部阻断，后端服务 `/ssapi/auth/*` 接口全面抛出 500 错误。无法进入系统主页。**

### 详细错误日志：
- [API Error] 500 Internal Server Error on `http://localhost:9090/ssapi/auth/code` (验证码/租户加载阶段)
- [API Error] 500 Internal Server Error on `http://localhost:9090/ssapi/auth/tenant/list`
- [API Error] 500 Internal Server Error on `http://localhost:9090/ssapi/auth/login` (提交账号密码时)

### UI 表现：
在登录页面输入任何提供的账号和密码后，页面会弹出一个遮罩层的系统错误提示弹窗：
> 系统提示：服务器异常(500)
> 知道了

点击“知道了”后停留在登录页，导致后续的“家长中心”和“洞察”菜单无法被测试和访问。

### 需解决的优先级问题：
1. 修复后端 API 的 500 异常，特别是 `auth` 相关接口，恢复登录功能。
2. 待登录恢复后，重新运行自动化测试脚本（已编写 `e2e_all_accounts.js` 准备就绪）。