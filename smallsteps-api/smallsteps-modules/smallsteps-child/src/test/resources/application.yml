spring:
  profiles:
    active: dev
  datasource:
    dynamic:
      datasource:
        master:
          url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:15432}/${DB_NAME:smallsteps_db}
          username: ${DB_USER:smallsteps}
          password: ${DB_PASS:ui123456789~}
          driverClassName: org.postgresql.Driver

mybatis-plus:
  mapperPackage: com.kenzhao.smallsteps.**.mapper

ai:
  vllm:
    baseUrl: http://192.168.1.9:8002/v1
    apiKey: ken2zhao589612
