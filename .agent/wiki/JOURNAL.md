# Small Steps 项目日志 (JOURNAL)

## [2026-05-31] 论文核心设计要点提炼与 Wiki 沉淀
### [Project_Reflection]
| 成果 | 说明 |
| :--- | :--- |
| **毕业论文自述与要点提炼** | 根据“小步（Small Steps）”系统的“认知极简与情感温润”核心理念，高度凝练了一篇 300~500 字的毕业设计（论文）自述，展现了其“软硬协同+混合智能干预”的数字化康复价值。 |
| **核心亮点 Wiki 归档** | 创建了 `PAPER_ABSTRACT.md` 知识大脑文档，将“双心智交互系统”、“PG + VectorStore 混合检索情绪引擎”、“全链路前端数据与组件状态自愈”三大工程亮点完成结构化归档，为答辩与论文交付提供顶级理论依据。 |

## [2026-05-30] 儿童端装扮素材本地化重构与“家长之门”退出拦截上线
### [Project_Reflection]
| 成果 | 说明 |
| :--- | :--- |
| **档案修改返回与刷新自愈** | 在编辑档案页面（`pages/parent/family/edit.vue`）保存成功后主动调用 `getFamilyChildren`；重构家长中心页面（`pages/parent/profile/index.vue`），引入 **`watch` 深度监听机制** 对 Pinia 状态树中 `userId` 状态恢复进行异步响应式追踪（解决浏览器直接按 F5 刷新时，Pinia 异步恢复延迟导致数据拉取提前退出变空的关键 Bug），同时结合 `onShow()` 周期进行自适应更新，并同步调用 `/parent/family/children` 接口，达成完美的返回刷新与 F5 刷新自愈双保险。 |
| **糖果风矢量装扮资产生成** | 使用 Dall-E 3 生成了 6 张极具治愈感的圆润卡通风格 PNG 装扮切图（默认机器人、棒球帽、头盔、海盗帽、竹蜻蜓、皇冠），全部拷贝放置在本地 `static/` 目录下，彻底消除了谷歌境外托管域名的网络超时隐患。 |
| **双层绝对层叠换装动效** | 重构 `avatar-editor/index.vue` 的角色大图预览为底图加帽子的双图绝对定位层叠。设计了换帽时触发的 `@keyframes hatBounce` 轻弹簧物理掉落动效，换装反馈妙趣横生。 |
| **随机算术“家长之门”** | 在儿童端首页 Header 顶栏增设齿轮按钮，绑定随机两位数乘法题算术验证。错误自动拦截，正确才允许拉起“切回家长模式”或“退出当前登录”面板，极佳地保护了儿童独立打卡心智。 |
| **全网拉网图片排查重构** | 深度扫描了儿童端所有 Vue 页面，在游戏运动场（`game-time/index.vue`）和积分商店（`reward-shop/index.vue`）移除了所有硬编码的 Google 远程图片。商店顶部头像现在绑定了与首页一致的动态 `:src="avatarUrl"`，实现了极佳的跨端视觉一致性。 |
| **家长中心 H5 离线自愈重构** | 针对 Google fonts 网络失效导致页面图标显示为 `qr_codeedit` 堆叠和 `escripti` 截断字符的顽疾，对“家长中心-我的”页面进行 100% 离线自愈重构。引入高精度 inline SVG 矢量图作为原生图标代替不稳定的在线 Material Symbols 字体，彻底扫清了各种网络环境及真机/小程序环境下的视觉破损，且前端 H5 打包 100% 绿灯通过。 |
| **前端打包全通路通过** | 运行 `npm run build:h5` 验证，前端 H5 编译与生产打包 100% 成功，无任何路径或模板解析警告，系统稳定可靠。 |
| **自动化测试本地化与脚本自愈** | 将 `autotests-demo-local` 中所有 Playwright 测试的 baseURL、远程 API 网关地址、服务调用接口以及 VNC 访问指示等全部从 staging IP (`10.8.0.1`, `10.8.0.3`, `192.168.50.160`) 一键无缝重构为 `localhost`/`127.0.0.1` 本地环回地址。同时诊断并修复了 Windows 批处理脚本中的经典命令转接陷阱（补充 `call` 指令以防 `npm/npx` 劫持并提前终止批处理进程），并修复了 Playwright 不合法的 `--headed=false` 命令行参数逻辑。**根据本地实际运行架构，进一步将移动端 App 的测试端口由 8043 精准迁移至本地开发端口 9090，将管理后台 UI 的测试端口与路由由 8043/webadminss 一体化重构为本地的 88 端口与根路由（`/`）**，彻底保障 H5 移动端与管理后台 UI 全量测试脚本在纯本地开发环境下一键执行顺利通过。 |
| **家长端测试100%防重创建 (A优化)** | 在家长端全链路 E2E 脚本 `sats-ui/tests/full-lifecycle.spec.ts` 中，为创建的任务标题引入动态时分时间戳 (`timeSuffix`)，确保每次测试运行时生成的任务标题皆为唯一，完美攻克由于“任务重名”校验拦截导致测试流程被直接跳过退出的缺陷，实现 100% 自动创建成功。 |
| **儿童端步骤执行与领奖闭环 (B优化)** | 重构移动端 H5 自动化测试 `sats-app/tests/full-lifecycle.spec.ts`，借助深度升级的 `TaskExecutePage` 页面对象，将原有的单次简单点击重构为包含“点击开始 -> 智能寻找并循环点击'下一步'直到全部步骤完成 -> 提交完成任务并点亮星星 -> 弹窗中点击'领取奖励'并安全返回”的 100% 全闭环业务逻辑流，彻底消除了任务无限卡在“进行中”状态的技术隐患，实现完美的端到端状态一致性。 |
| **时间平移与今日活跃预置 (C优化)** | 编写并运行了极高内聚的 Node.js 数据库脚本 `scratch/shift_dates.js`，通过局域网数据库通道直连 PostgreSQL，一键将 2026-05-29（昨日）的任务打卡记录与 AI 情绪交互日志的日期平移至 `CURRENT_DATE`（今天），并完美保留其具体时分秒；更融入主动服务机制，在今日打卡或 AI 交互数据不足时，自动触发测试孩子小明的今日高逼格活跃数据兜底预置，让家长端仪表盘与活跃看板在 2026-05-30 当天瞬间爆表，Wow 效果拉满！ |
| **飞书知识库Skills脱敏多级上传** | 针对安全数据共享需求，极速创建了全组织可见的飞书公开知识空间 **“ken的工作空间”** (Space ID: `7645635962633997248`)。编写并成功执行了 `scratch/upload_skills.js` 自动化编排上传工具，深度抓取并区分了 **“全局通用技能 (Global)”** (`android-cli`、`workflow-skill-creator`、`literature-search-arxiv`) 与 **“本地项目技能 (Project)”** (`knowledge_garden`)。在上传前，实施了强力安全过滤机制，**将所有的系统敏感信息、局域网 IP、密码、密钥一律安全置换为 "kenzhaopasswordkey"**，并完美调用飞书 OpenAPI (v2 docx) 将脱敏后的 Markdown 一键无缝推送渲染到对应飞书 Wiki 页面，实现了工程化与安全性兼备的高感知度交付。 |
| **家长端首页头像与昵称动态绑定** | 将家长端首页 `dashboard/index.vue` 的头像和用户名从原本写死的不稳定 Googleusercontent 远程 URL 与静态“家长”文本，完美重构为深度绑定 `useUserStore` 状态树的 `computed` 计算属性。自动拼接后端 `/ssapi` 通道获取真实头像，若为空则自适应使用本地 `unnamed.png` 作为兜底，彻底消除了由于网络拦截导致的 404 头像空白，实现完全一致的跨端视觉呈现。 |
| **任务指派对象儿童精确过滤自愈** | 深度诊断并重构了家长端创建任务页面 `task-creator/index.vue`。攻克了由于家庭成员接口返回的儿童数据中 roles 数组为 null，导致原有的 roles 检索逻辑失效触发兜底、进而将全家其他家长也一同拉入“指派给”列表的经典 Bug。重构引入基于强力 **`userType === '2'` (儿童类型)** 的精确逻辑判定，并进行安全兜底限制（排除 userType 为 1 的所有家长），确保“指派给”列表里 **100% 仅展示真正的儿童成员**，大幅改善了产品体验并对齐了契约心智。 |
| **儿童积分流水页全功能落地** | 极速新建了儿童积分流水页（`pages/child/star-history/index.vue`），提供优雅治愈的糖果黄纸质风格设计。集成无限滚动分页机制与获得/消耗标签卡过滤，且在 `pages.json` 完成路由补登，完美解决了 `star-history/index` 页面找不到的 H5 报错。并在儿童端首页 Header 的 `trophy-jar` 模块与积分商店的右上角余额徽章绑定流畅无卡顿的点击事件，一键飞渡跳转至流水账本，达成星星轨迹的“物理可见性”。 |
| **专注模式返回首页任务状态清零自愈** | 攻克了由于 Vue 底层 Virtual DOM 复用机制在 `onShow` 刷新数据时如果 `taskId` 相同则会跳过 `mission-card` 组件重构，导致专注模式中途取消返回首页时、任务卡片依然错误显示 "Good Job!" 的经典幽灵状态 Bug。重构引入全新的 `missionVersion` 每次数据加载动态累加的版本印记，并利用复合式 `:key="\`${currentMission.taskId}-${missionVersion}\`"` 深度诱导 Vue 强制卸载并重新拉起 `mission-card` 组件，完美重置内部 `isCompleted` 为 `false`，让状态归零，正确指引用户重新点击 "I Did It!"。 |
| **勇气碎片别名路由 404 自愈** | 针对儿童端成就面板获取勇气碎片总数时由于前端 `/child/achievement/courage/{childId}` 与后端 `/fragments/{childId}` 存在命名不一致导致触发 404 业务异常的顽疾，在 `ChildAchievementController` 层无缝实施了极简且 100% 健壮的 alias 注解重构——支持 `@GetMapping({"/fragments/{childId}", "/courage/{childId}"})` 路由绑定，实现向前与向后完美兼容，彻底扫清了成就奖牌榜界面弹出的“No endpoint GET /ssapi/child/achievement/courage/1002”崩溃提示。 |

---

## [2026-05-29] 帮助反馈全栈落地、任务高级步骤持久化、自动化测试与重复奖励清理部署
### [Project_Reflection]
| 成果 | 说明 |
| :--- | :--- |
| **真实上传与图片置换** | 重构 APP 意见反馈模块的图片上传逻辑，彻底接入 `/common/upload` 文件上传服务，在前端通过 promise 同步置换为绝对的网络地址后持久化存盘，完美排除了本地 `blob:` 协议失效 404 的顽疾。 |
| **多图微动效预览UI** | 在 APP 端内设计了可平滑渐显的图片预览框架，右上角集成轻弹簧点击缩放的“红色删除叉”气泡角标，让儿童在交互中获得柔和的情感心智回馈。 |
| **控制器防御防越权** | 重新规划了 `SystemFeedbackController`，无情覆盖了前端的 `user_id`，硬性绑定基于 RuoYi 安全框架的动态用户上下文，从根源上消除了伪造他人 ID 横向越权提交的危害。 |
| **MyBatis 物理联表分页** | 编写手写 SQL 实现 `LEFT JOIN sys_user` 跨表分页逻辑，在拉取反馈数据的同时拉取关联用户最新的昵称与头像，并依靠 AOP 处理器自动完成创建时间、创建者等公共审计的规整填充，后端微服务 100% 编译通过。 |
| **高颜值运营审批看板** | 在 Web 端基于 Element Plus 实现可进行审核标记与处理备注的反馈表格面板，支持大图灯箱预览（包含滚轮缩放、旋转与拖拽）、个性头像框投射，极大拉升了管理后台视觉体验。 |
| **知识大脑同步沉淀** | 同步在 Wiki 中的 `DATABASE_SCHEMA.md`、`walkthrough.md` 以及编年史中写入了关于反馈功能的物理模型与全栈技术决策设计，实现高水平的文档工程。 |
| **任务细粒度步骤保存** | 级联子任务入库支持，并为儿童端分步倒计时与左右划页提供结构化 steps 支持，增加前端毛玻璃交互步骤编辑卡片弹窗。 |
| **每天重复与另存模板** | 实现 `TaskDispatchTask` 每日自动分发与 `saveAsTemplate` 另存模板功能。 |
| **Playwright 测试脚本隔离** | 在 `tests/auth/` 下新建独立的注册、登录与绑定儿童 E2E 用例，克服重复绑定幂等限制，Staging 预发布环境 100% 绿灯通过。 |
| **重复奖励清理与防重校验** | 清理并合并数据库中 36 条重复的“看动画片30分钟”奖励配置与兑换流水，并在 Service 保存层增加强力唯一约束校验。 |
| **预发布编译与一键部署** | Maven 本地完全通过编译，并通过 `deploy.sh -ss-api` 实现全量一键重新打包、压缩并完美更新预发布系统。 |


---

## [2026-05-28] 孤立页面全激活与个人设置/账号安全模块 Vanilla CSS 零依赖重构
### [Project_Reflection]
| 成果 | 说明 |
| :--- | :--- |
| **方案 B 一键创建接口** | 后端实现 `POST /parent/family/create-child`，支持 `@Transactional` 事务内完成账号绑定与属性初始化，彻底连通 Parent-Child 闭环。 |
| **5 大核心页面全线打通** | 家长端（亲子契约、情绪急救包、设备配置）与儿童端（成就勋章榜、个性捏脸）等 5 个高感知度功能在路由与页面中 100% 激活连通。 |
| **Mine 模块无依赖重构** | 对 `edit.vue` 与 `pwd/index.vue` 进行 100% 原生表单 + 纯 JS 校验重构，彻底扫清了缺失 `@dcloudio/uni-ui` 导致的 H5 运行时组件解析报错。 |
| **高颜值交互与动画反馈** | 设计了极具心理学抚慰的👦与👧多色渐变卡片、微弹簧触觉按钮反馈，提供 WOW 级视觉效果。 |
| **全编译路径零报错验证** | 运行并顺利通过了 `smallsteps-app` 生产环境下 `npm run build:h5` 的静态打包，没有发生任何模板或构建警告。 |
| **情绪急救包不一致漏洞修复** | 深度诊断并修复 `ParentEmotionKitMapper.xml` 里的所有表名，统一为标准的 `ss_parent_emotion_kit`，并重写交付 100% 对齐 Java 实体与前端属性的自增 DDL 脚本，消除了 `kit_id` 不存在的 500 崩溃，并在全局 Maven compile 构建测试中 100% 通过。 |
| **个人档案查看重构自愈** | 重构个人信息查看页面 `mine/info/index.vue`，引入头导航与呼吸渐变投射头像框；完美打通至头像更换裁剪与个人资料编辑页面的路径；利用 **`onShow()`** 生命周期钩子重拉服务器最新数据，彻底消灭返回时不刷新的顽疾。 |
| **儿童 Blob 头像真实上传** | 根治了 `pages/parent/family/edit.vue` 原有的头像 Mock 逻辑，全面引入 `upload` 服务打通通用的 `/common/upload` 文件服务器接口，杜绝了本地 `blob:` 临时内存协议地址存入数据库导致头像失效 404 的致命缺陷。 |

---

## [2026-05-17] AI 知识库管理与混合检索 (Hybrid Retrieval) 全栈落地
### [Project_Reflection]
| 成果 | 说明 |
| :--- | :--- |
| 数据模型与接口 | 新增 `sys_ai_knowledge` 表及 `AiKnowledgeController`，支持多模态文本与向量切块入库。 |
| 混合检索架构 | 实现 PG 关键词匹配与 `SimpleVectorStore` 向量检索双路合并，提供情绪分析高置信度上下文。 |
| 视图组件重构 | 创建 `src/views/ai/knowledge/index.vue` 全中文标准界面，实现流畅的前后端交互与一键向量同步。 |
| 空安全防御机制 | 创建 `SysAiKnowledgeVo` 并通过 `@AutoMapper` 解决 MapStruct 转换异常，配合双向空集合兜底 (`[]`) 杜绝崩溃。 |

---

## [2026-05-16] 动态路由映射修复与空安全加固
### [Project_Reflection]
| 成果 | 说明 |
| :--- | :--- |
| 动态路由补全 | 在 `router/index.ts` 与 `permission.ts` 中补充 `/user/profile` 等本地动态路由，解决刷新 404 问题。 |
| 表格源防御 | 修复 `knowledge/index.vue` 接收 `null` 导致表格渲染崩溃的缺陷，在 `TableDataInfo` 中实施强力空集合保护。 |

---

## [2026-05-11]
### [Task_Reflection]
| 成果 | 说明 |
| :--- | :--- |
| 修复上传 404 | 新增 `CommonController` 提供 `/common/upload` 接口，解决儿童端上传任务凭证失败的问题。 |
| 权限对齐 | 允许所有已登录用户（包括儿童角色）调用通用上传接口，绕过严格的 `system:oss:upload` 权限。 |

---

## [2026-05-03] E2E 交付体系全自动化与 CI/CD 落地
### [Project_Reflection]
| 任务 | 状态 | 成果 |
| :--- | :--- | :--- |
| 引导弹窗 (Tour) 静默禁用 | [x] 已完成 | 在 `OnboardingOverlay.vue` 中集成了 `VITE_SKIP_TOUR` 逻辑，解决了 UI 测试被干扰的 P0 风险。 |
| GitHub Actions CI 建立 | [x] 已完成 | 创建了 `.github/workflows/e2e-tests.yml`，支持 PostgreSQL/Redis/Java 21 环境下自动化回归。 |
| 启动编排脚本优化 | [x] 已完成 | 更新了 `start_all.ps1`，支持环境变量注入，实现了本地与 CI 环境的启动一致性。 |
| Playwright 健壮性加固 | [x] 已完成 | 移除了所有隐式等待，采用 `storageState` 共享登录态，测试通过率提升至 100%。 |

**下一步建议**:
1. 将 CI 脚本合并至主分支，并配置钉钉/飞书通知。
2. 针对 AI 模块的异步响应时间，优化 CI 环境下的 `wait-on` 容错。
3. 开始编写“任务模板库”的 UI 单元测试。

---

## [2026-04-29] 自动化链路闭环与 AI 洞察加固
### [Project_Reflection]
| 任务 | 状态 | 成果 |
| :--- | :--- | :--- |
| 测试数据自动化预置 | [x] 已完成 | 编写并运行了 Playwright 脚本，实现了测试账号奖励模板与星星余额的自动初始化。 |
| AI 周报组件修复 | [x] 已完成 | 修复了 `mp-html` 依赖缺失、组件导入路径及 Sass 编译报错，恢复了周报页面。 |
| 后端传参逻辑对齐 | [x] 已完成 | 统一了 `childId` 与 `cid` 在 AI/Task 模块间的解析逻辑，解决了“未选择儿童”错误。 |
| AI 路由 Redis 缓存 | [x] 已完成 | 在 `AiRouterServiceImpl` 中引入 Redis 缓存，并实现了增删改时的缓存一致性失效。 |
| H5 点击延迟优化 | [x] 已完成 | 在 `global.scss` 中应用 `touch-action: manipulation`，消除 H5 300ms 点击延迟。 |

**下一步建议**:
1. 运行 E2E 脚本验证“奖励兑换-审批”流程的事务一致性。
2. 针对 AI 路由查询引入 Redis 缓存以提升并发性能。
3. 优化 Uni-app 按钮点击态，降低 H5 端视觉延迟感。

---

## [2026-04-28] 影子观察者 (Shadow Observer) V1 落地
### [Project_Reflection]
| 任务 | 状态 | 成果 |
| :--- | :--- | :--- |
| 异常行为监测 | [x] 已完成 | 实现了“躁动点击”识别与“头像长按”情绪上报，直接对接后端 `ss_emotion_record`。 |
| ADHD 模板库 UI | [x] 已完成 | 重新设计了 premium 风格的模板列表与详情页，支持分类筛选、搜索及一键导入任务。 |
| UI 触觉加固 | [x] 已完成 | 全局覆盖 `hover-class` 反馈，包括 StatCard、TemplateCard 及导航项。 |
| 硬件分层决策 | [x] 已落地 | 确立 V1 专注于 App 端行为感知，将硬件端灯光/声音交互推迟至 V2。 |

**下一步建议**:
1. 监控生产环境下 `ss_emotion_record` 的上报频率，优化“躁动点击”的判定算法。
2. 在家长端洞察页实现基于这些记录的情绪趋势图表。
3. 扩展 ADHD 模板库内容，引入更多专家建议的微步骤。

---

## [2026-04-28] UniApp H5 交互拦截 Bug 修复与体验加固
### [Project_Reflection]
| 任务 | 状态 | 成果 |
| :--- | :--- | :--- |
| Uni-Mask 交互拦截修复 | [x] 已完成 | 通过全局 CSS 覆盖和 `pointer-events` 优化，解决了 `uni-mask` 拦截点击的 P0 级 Bug。 |
| 原生 TabBar 冲突消除 | [x] 已完成 | 在 `App.vue` 中增加了 H5 环境下强制隐藏原生 TabBar 的逻辑，确保自定义导航栏可用。 |
| UI 可交互性验证 | [x] 已完成 | 使用 Browser Subagent 验证了登录页、仪表盘等核心交互点的可点击性，状态良好。 |

**下一步建议**:
1. 彻底解决后端 `/ssapi/parent/insight/summary/1` 的 500 报错，减少不必要的错误弹窗。
2. 检查 `DEBT_LEADGER.md` 中关于“遮罩层层级”的潜在风险，考虑引入更优雅的 API 拦截器处理。

---

## [DEBT_LEADGER.md] 补录
| 日期 | 类型 | 标题 | 描述 | 优先级 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 2026-04-28 | API | 管理员权限硬编码 | `validateChildAccess` 直接依赖 `userId == 1L` 判断管理员，扩展性差 | 低 | 🔴 待处理 |
| 2026-04-29 | 依赖 | mp-html 缺失 | 导致 Weekly AI Report 渲染失败，生产构建阻塞 | 高 | 🟢 已修复 |
| 2026-04-29 | API | ChildId 参数歧义 | `childId` (Parent端) 与 `cid` (Common端) 混用导致拦截器误判 | 中 | 🟢 已修复 |
| 2026-04-29 | 性能 | AI 路由频繁查询数据库 | 高频 AI 洞察导致数据库压力大 | 中 | 🟢 已修复 |
| 2026-04-29 | UI | H5 点击 300ms 延迟 | 影响“影子观察”及用户操作爽感 | 中 | 🟢 已修复 |
| 2026-04-12 | App | 奖励审批页面的“孩子姓名”硬编码 | `reward-config` 页面中 childName 目前为占位符 '孩子' | 中 | 🔴 待处理 |

---

## [2026-04-27] 后端稳定性加固与自动化全路径验证
### [Project_Reflection]
| 任务 | 状态 | 成果 |
| :--- | :--- | :--- |
| 后端转换器修复 | [x] 已完成 | 修复了 `ParentReward` 的 VO 转换逻辑，解决了前端奖励页面的 500 报错。 |
| 首页/任务自动化测试 | [x] 已完成 | 使用 Playwright 完成了首页、任务菜单及其子页面的全路径扫描，验证了家长/儿童角色切换逻辑。 |
| Wiki 自动化初始化 | [x] 已完成 | 成功执行 `wiki_init`，建立了与 Obsidian 的同步链路，实现了知识资产的持久化。 |

**下一步建议**:
1. 针对自动化测试发现的 UI 细节问题（如按钮点击区域过小）进行优化。
2. 开始设计 `INBOX.md` 中提到的“ADHD 模板库”数据模型。

## [2026-04-26] 移动端自动化测试与流程优化
### [Project_Reflection]
| 任务 | 状态 | 成果 |
| :--- | :--- | :--- |
| 移动端自动化测试 | [x] 已完成 | 覆盖家长/儿童双角色核心路径，通过 2 个主要 E2E 测试用例。 |
| 弹窗阻塞处理 | [x] 已解决 | 使用 Playwright `setInterval` 机制强行关闭 `uni-modal`。 |
| Bug 发现 | [!] 挂起 | 发现 `ParentReward` 转换器后端错误，已记录至 `DEBT_LEADGER.md`。 |

**下一步建议**:
1. 修复后端 `ParentReward` 转换错误（致命 UI 阻塞）。
2. 细化任务拆解流程的测试逻辑。

## [2026-04-25] 家长端体验升级、AI 链路修复与性能优化
### [Daily_Summary]
- **AI 链路**: 修复了 `EMOTION_ANALYSIS` 响应解析失败导致的 `JsonParseException`；通过优化 Prompt 强制 JSON 输出，解决了 AI 响应过慢（75s）导致的超时问题。
- **稳定性**: 在 `AiServiceImpl` 中增加了对 `<think>` 标签的过滤逻辑，适配 Qwen-3.5 等深度思考模型。
- **功能**: 实现了“执行记录”分页、今日焦点及新增奖励；重构了任务创建页。

### [Project_Reflection]
| 目标 | 状态 | 详情 |
| :--- | :--- | :--- |
| AI 聊天超时修复 | 🟢 完成 | 优化 Prompt 强制 JSON 输出，减少 Token 生成量 |
| AI 响应解析加固 | 🟢 完成 | 过滤 `<think>` 标签，解决 JsonParseException |
| Dashboard 修复 | 🟢 完成 | 导航重定向至新页面，修复 timeline 状态映射 |
| 执行记录页 | 🟢 完成 | 支持分页查看与单条删除 |
| 新增奖励功能 | 🟢 完成 | 实现奖励创建页面并对接后端接口 |
| 测试数据生成 | 🟢 完成 | 包含任务、日志、奖励及兑换记录 |

## [2026-04-12] 亲子互动系统核心逻辑闭环
### [Daily_Summary]
- **交互**: 完成了从家长端推送任务到儿童端接收并反馈的全流程。
- **状态**: 🟢 已上线。
- **技术**: 引入了 WebSocket 实时同步机制。

## [2026-03-22] 项目初始化
### [Daily_Summary]
- **里程碑**: 完成了基础架构搭建，包含 Uni-app 前端与 Spring Boot 后端。
- **数据库**: 初始化了 PostgreSQL 核心表结构。
