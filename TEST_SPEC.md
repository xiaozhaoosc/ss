# Small Steps 项目测试方案与规范

## 1. 测试工具建议：Playwright vs. MCP

针对本项目，我们建议采用 **Playwright** 作为核心自动化测试框架。

| 维度 | Playwright (推荐) | MCP (Chrome DevTools) |
| :--- | :--- | :--- |
| **适用场景** | 持续集成 (CI)、回归测试、端到端 (E2E) 流程。 | 探索性测试、AI 辅助调试、动态环境分析。 |
| **优势** | 跨浏览器支持、速度快、断言丰富、支持录制视频。 | 无需编写脚本即可操作、深层集成到 AI 思维链。 |
| **可维护性** | 高（代码版本化管理）。 | 低（依赖 AI 指令的即时执行）。 |

**结论**：使用 Playwright 编写核心业务流（如注册、发布任务、完成任务）的测试脚本；使用 MCP 进行临时的功能验证和 Bug 扫描。

---

## 2. 核心功能测试矩阵 (Test Matrix)

### A. 移动端 (Uni-app / H5)
- [x] **身份持久化**: 刷新页面后 Token 是否保留，用户信息是否正确回显。
- [x] **儿童数据隔离**: 确保儿童 ID 为 1002 的用户无法访问 ID 为 1003 的任务数据。
- [/] **AI 对话流**: 验证 SSE (Server-Sent Events) 的解析与渲染。 (目前发现 404 问题需修复)
- [ ] **时光机**: 验证情绪历史数据的图表展示。

### B. 后端 API (Spring Boot)
- [x] **Sa-Token 鉴权**: 所有 `/ssapi/child/**` 接口必须校验身份。
- [x] **数据校验**: 新增任务时，`parent_id` 是否为当前登录用户的真实 ID。

### C. 管理端 (Vue3 + Element Plus)
- [ ] **仪表盘**: 验证实时统计数据的准确性。
- [ ] **任务管理**: 验证管理员对所有任务的增删改查权限。

---

## 3. 已执行的自动化测试 (2026-04-26)

我们已使用 MCP 工具执行了以下自动化扫描：
1. **[PASS] 状态丢失修复验证**: 通过刷新页面检查 `top-bar` 姓名回显。
2. **[PASS] 权限修复验证**: 直接调用儿童数据接口并捕获响应。
3. **[FAIL] AI 对话验证**: 发送请求后捕获到 404 错误。

---

## 4. 下一步计划 (TODO)
1. 修复 `GET /ssapi/child/ai/chat/stream` 404 错误。
2. 完善家长端“时光机”入口缺失问题。
3. 部署 Playwright 环境并录制第一个 E2E 测试脚本。
