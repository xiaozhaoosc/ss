# 项目编年史 (CHRONICLE)

## 2026-05-29: 全系统雪花长 ID 统一字符串化拦截方案与家长中心 Bug 最终治理
- **全系统长 ID 统一无损字符串化**：针对 19 位雪花算法 Long ID 在前端（APP / Web）中因 JS 浮点精度限制而被强制截断为 `...1300` 造成的死锁性 Bug，在 `smallsteps-common-json` 模块的 `JacksonConfig` 最底座中将 `BigNumberSerializer` 显式且坚固地注入 `Jackson2ObjectMapperBuilderCustomizer` 构建器。所有雪花长整型在出参时自动转化为 String，入参时自动无缝反序列化，以 0 业务代码侵入的最小化方案实现了前后端 100% 透明兼容，并发布了架构决策记录 [[ADR-020-Unified-Snowflake-Id-Stringification]]。
- **家长中心儿童档案及雷达图回归**：废除了 `SysUserVo.java` 和 `Child.java` 实体上手写的手工序列化注解；在 `ChildServiceImpl.java` 服务层对 `selectChildList` 注入防卫逻辑，强制普通已登录家长的 `parentId` 为真实当前登录用户，彻底攻克了“创建儿童后列表显示为空”与雷达能力多维图 `500 无权访问` 的隐患。

## 2026-05-17: AI 知识库管理功能重构与全栈异常防御治理
- **前后端双向空安全防御**：针对动态编译时缺少 MapStruct VO 转换类导致底层返回 `null` 的极端情况，在后端基类 `TableDataInfo` 和前端 `knowledge/index.vue` 实施了严格的空集合防御（短路求值与默认实例化 `[]`），彻底杜绝了 Element Plus 表格崩溃及“数据保存失败”的假象。
- **AI 知识库 CRUD 全栈贯通**：完成了 AI 知识库界面的全中文 RuoYi 规范 CRUD 对接与向量库同步状态流构建，并发布了架构决策记录 [[ADR/ADR-018-TableDataInfo-Null-Safety-Defense|ADR-018]]。

## 2026-05-11: 生产环境配置硬化与全栈 API 对齐
- **API 路径标准化**：统一了 `smallsteps-app` (Mobile) 和 `smallsteps-ui` (Web) 的生产环境 API 前缀为 `/prod-api/`，并通过 `.env.production` 实现了配置持久化。
- **容器间通信修复**：修正了 Nginx 代理配置中遗留的 `backend-core` 主机名，将其与 `docker-compose.yml` 中的 `api` 服务名对齐。
- **监控组件解耦**：针对生产环境下由于 Admin Server 未部署导致的 `ConnectException` 日志污染，将 `spring-boot-admin-client` 和 `snail-job` 设为默认关闭，并支持通过环境变量动态开启。
- **环境鲁棒性增强**：通过 `${VAR:default}` 语法确立了“配置容错”范式，解决了容器化环境下 `localhost` 回环连接无效的问题。

## 2026-05-11: Sass 构建架构现代化与警告清零
- **Sass 3.0 迁移**：攻克了 `smallsteps-app` 中由于 `@import` 弃用导致的构建警告，全量迁移至现代的 `@use` API。
- **全局变量注入优化**：通过在 `vite.config.js` 中引入 `uni` 命名空间，解决了多文件 Sass 模块间的变量冲突（Collision），实现了 H5 端 100% 绿色构建。

## 2026-05-08: 后端驱动的数据一致性加固与多子女洞察闭环
- **数据一致性修复**：攻克了星星余额显示不一致的顽疾，确立了“后端驱动数据真理”的原则，消除了前端硬编码 Fallback 带来的误导。
- **多子女洞察体验**：在“家长洞察”页面实现了水平滑动切换组件，并重构了 `ChildAIController` 权限链，使其基于 `DeptId` 兼容家庭多成员访问。
- **环境配置硬化**：清理了 Vite/Sass 启动时的无效废弃告警，提升了开发环境的清洁度。

## 2026-05-08: 儿童端任务执行回归修复与数据底座加固
- **多租户兼容性修复**：解决了 `BaseEntity` 审计字段注入在儿童账号（缺失 `deptId`）下的 NPE 崩溃，防止了 401 Unauthorized 导致的交互死锁。
- **任务状态机闭环**：修正了后端查询逻辑，使其兼容 `status='0'` (初始/待领取) 状态，解决了指派任务对孩子不可见的难题。
- **UI/UX 鲁棒性增强**：在 `task-execute` 页面引入了显式 loading、错误 Toast 及详细日志，提升了在弱网环境下的系统韧性。
- **数据一致性清理**：通过 SQL 补丁同步修复了存量脏数据的审计字段与逻辑删除标记，确保全量数据可见。
- **高保真仪表盘升级**：重构家长首页，集成 ECharts 能力雷达、行为热力图及 AI 资源监控，解决了图标 404 及数据死板问题。
- **E2E 自动化测试闭环**：基于 Playwright 构建了仪表盘专项测试套件，解决了新手引导干扰及端口配置冲突，达成 100% 测试通过率。

## 2026-05-05: 系统加固与硬编码风险审计
- **全量配置审计**：对全栈（UI, API, App, ESP32）进行了深度硬编码扫描，识别了 4 大类高危风险点，并沉淀了《硬编码审计报告》。
- **E2E 噪声消除**：在自动化测试环境中永久禁用了 Intro.js 引导动画，解决了 UI 自动化测试中最大的不稳定性来源。
- **配置标准化启动**：确立了从硬编码 IP 向环境变量（Spring Profiles / Vite Env）迁移的加固路线。

## 2026-05-03: 家长端体验闭环与数据底座加固
- **数据库加固**：针对 `ss_task_template` 等核心业务表补齐了 `BaseEntity` 审计字段（`create_dept` 等），解决了 MyBatis-Plus 自动填充失败导致的插入错误。
- **家长中心 UI 3.0**：重构了家长端“个人中心”，引入毛玻璃 (Glassmorphism) 视觉规范，整合了通知、隐私、安全及账号注销等模块。
- **AI 情感助手集成**：在“帮助与反馈”模块中成功接入 AI 情感助手，实现了基于 SSE 的流式对话，为家长提供专业的 ADHD 育儿指导与情绪支持。

---

记录重大架构变迁、关键决策背景（ADR）及项目里程碑。

## 序言
这里记载了“Small Steps (小步)”从雏形到成为 ADHD 儿童成长伙伴的每一公里。

---

### [2026-05-02] E2E 自动化测试架构现代化与稳定性加固
- **事件**: 
    1. **稳定性飞跃**: 彻底移除了不稳定的 `networkidle` 等待机制，采用显式的元素/状态等待，解决了导航超时问题。
    2. **会话隔离**: 优化了测试过程中的 Cookie 与 Storage 管理，解决了 Parent 和 Child 角色间的登录态冲突。
    3. **覆盖率扩展**: 在 `smallsteps-ui/tests` 中新增了多个核心业务流测试用例。
- **产物**: 
    - **测试脚本**: 优化后的 `e2e_all_accounts.js` 及各模块专属测试文件。
    - **逻辑对齐**: 修正了从 `/index` 到 `/dashboard` 的重定向逻辑，使测试环境与生产环境一致。
- **意义**: 标志着项目具备了“无人值守、高置信度”的持续集成验证能力，为后续的功能迭代提供了坚实的质量护城河。
- **关联**: [[b19be268-E2E-Stabilization]] / [[19b481ec-Coverage-Expansion]]

### [2026-04-30] 自动化测试框架升级与业务逻辑硬化
- **事件**: 
    1. **E2E 稳定性提升**: 解决了 POM (Page Object Model) 冲突，将不稳定的 `waitForTimeout` 替换为显式等待。
    2. **UI 健壮性修复**: 修复了 Vue 组件中 `totalPoints` 等变量未定义导致的 ReferenceError。
    3. **业务过滤增强**: 实现了功能单元配置的服务端多维过滤（产品类型、组码类型）。
- **产物**: 
    - **测试套件**: 现代化的 Playwright 测试脚本。
    - **前端组件**: 修复后的 `exchange_record/index.vue` 等核心页面。
- **意义**: 系统进入“高质量交付”阶段，通过自动化测试闭环确保了核心业务逻辑的回归稳定性。
- **关联**: [[6667f496-cf34-464a-a5fa-1905505b80fe]]

### [2026-04-29] 新手引导系统与动态角色分发
- **事件**: 
    1. **Onboarding 系统**: 实现了基于 `clip-path` 的聚光灯引导组件 `OnboardingOverlay.vue`。
    2. **角色注册链路**: 验证了家长端注册流中的 `userType` 动态分发逻辑。
    3. **任务分配闭环**: 在任务创建 UI 中集成了孩子选择组件，打通了 `childId` 绑定链路。
- **产物**: 
    - **组件**: `OnboardingOverlay.vue` / `Confetti` 动画集成。
    - **交互**: 任务创建页面的“孩子选择器”。
- **意义**: 极大降低了新用户的认知门槛，并为后续的多孩子家庭模型提供了 UI 层面的支撑。
- **关联**: [[3d9d93d3-Onboarding]] / [[df9b3de8-User-Roles]]

### [2026-04-28] 后端参数对齐与周报加载故障修复
- **事件**: 
    1. 解决了时光机等页面因参数名（childId vs cid）不匹配导致的 500 报错。
    2. 解决了家长端周报页面由于依赖缺失（mp-html）和 Sass 语法错误导致的加载故障。
- **产物**: 
    - **控制器兼容性增强**: 在 `ChildAIController` 等中实现了多参数联合解析。
    - **前端依赖修复**: 补全了 `mp-html` 依赖，重构了 `weekly-ai-report.vue` 的 CSS 深度选择器逻辑。
- **意义**: 彻底消除了家长端核心周报功能的访问障碍，增强了前端代码在现代 Vite/Sass 环境下的兼容性。
- **关联**: [[ADR-011-Backend-Parameter-Mapping-Fix]] / [[2026-04-28]]


### [2026-04-26] AI 聊天交互体验重构与稳定性加固
- **事件**: 攻克了 H5 开发环境下 AI 聊天流式响应（SSE）被截断的顽疾，并完成了 App 端登录态持久化与儿童权限链的深度修复。
- **产物**: 
    - **SSE 协议优化**: 实现了“首字节预热”机制，确立了开发环境绕过代理的绝对路径通信范式。
    - **权限控制**: 重写了 `validateChildAccess` 逻辑，实现了 100% 的数据所有权校验。
- **意义**: AI 交互从“可用”进化为“流畅”，且系统在多端刷新环境下的健壮性得到质的提升。
- **关联**: [[ADR-010-AI-Streaming-SSE-Optimization]] / [[2026-04-26]]

### [2026-04-25 (Session B)] 儿童端权限开通与异常修复
- **事件**: 彻底修复儿童端 403/500 报错，开放奖励商店权限，实现儿童端全流程闭环。
- **意义**: 解决了困扰 UI 层已久的权限链缺失问题，确保 ADHD 儿童用户能够顺畅访问奖励系统。

### [2026-04-25 (Session A)] AI 管理后台闭环与链路容错
- **事件**: 修复了导致线上 404 错误的强制路径变量配置，并解决了 `ParentTaskServiceImpl` 的 Lombok 编译障碍。
- **产物**: 
    - **API 规范**: 确立了可选 ID 传参范式，兼容路径变量与查询参数。
    - **持久层适配**: 将 `smallsteps-parent` 的 Entity 转换逻辑切换为 `BeanUtil`，规避了 Lombok 在复杂 Maven 继承下的失效风险。
- **意义**: 保证了系统在不同终端（移动端、PC端、Agent）请求时的极致健壮性，同时清零了阻塞 CI/CD 的技术利息。
- **关联**: [[2026-04-21]] / [[DEBT_LEADGER]]

### [2026-04-25] 家长端全功能闭环：档案管理与智能绑定
- **事件**: 交付了家长端“个人中心”全量功能，并重建了“孩子档案绑定”交互链路。
- **产物**: 
    - **页面**: `pages/parent/profile/index` (动态渲染) / `pages/parent/family/bind` (新页面)。
    - **API**: 封装了 `listChildren` 与 `bindChild` 业务逻辑。
    - **算法**: 实现了基于 birthday 的年龄动态推算及备注（remark）字段的结构化解析展示。
- **意义**: 标志着家长端从“单纯的任务管理”扩展到了“完整的角色与设备管理”生态，实现了 1:N 家庭模型在移动端的首次全功能着陆。
- **关联**: [[Parent-Profile-And-Family-Binding]] / [[2026-04-25-Parent-Profile-And-Binding]]

### [2026-04-25] AI 链路修复与排版优化 (Session A)
- **事件**: 修复了导致线上 404 错误的强制路径变量配置，并解决了 `ParentTaskServiceImpl` 的 Lombok 编译障碍。
- **产物**: 
    - **API 规范**: 确立了可选 ID 传参范式，兼容路径变量与查询参数。
    - **持久层适配**: 将 `smallsteps-parent` 的 Entity 转换逻辑切换为 `BeanUtil`，规避了 Lombok 在复杂 Maven 继承下的失效风险。
- **意义**: 保证了系统在不同终端（移动端、PC端、Agent）请求时的极致健壮性，同时清零了阻塞 CI/CD 的技术利息。
- **关联**: [[2026-04-21]] / [[DEBT_LEADGER]]

### [2026-04-14] 毕业论文文档化里程碑
- **事件**: 建立 `python-docx` 自动化流水线，批量生成符合上海应用技术大学格式规范的 `.docx` 论文文件。
- **产物**: v2 + v4 两版论文 Word 文档，覆盖 MySQL 技术栈（v2）与 Postgres 技术栈（v4）两种叙述版本。
- **意义**: 论文从内容撰写进入终稿排版阶段。标志着系统设计类毕设的"设计—实现—文档"三闭环贯通。
- **关联**: [[thesis_docx_pipeline]] / [[2026-04-14]]

### [2026-04-13] 系统功能开发收官
- **事件**: 完成任务管理最后一批功能：100%进度自动更新、强制最新进展字段、管理后台状态/团队标记同步、奖品兑换通知。
- **意义**: smallsteps-api / smallsteps-ui / smallsteps-app 三端核心功能进入冻结状态，转入论文写作与查重阶段。
- **关联**: 参见会话 `886188a7` (Enhancing Project Management System)

### [2026-04-13] SATS 与 AOT 排版协议确立
- **[DECISION]**: **SATS (SmallSteps Automated Testing Suite)** 设计方案定稿。
    - 确立了 API, UI, App, System 四位一体的自动化测试路径。
    - 引入 Superpowers 协议 (TDD/Verification) 作为核心质量门禁。
- **[AOT_LAYOUT_SETTLED]**: 深度研究 `chenglou/pretext` 源码，沉淀 AOT 排版策略。
    - **Docs**: 建立了 [[AOT_Layout_Strategy]]。
    - **Standards**: 将“两阶段排版”原则写入了 [[esp-idf-principles]]。
    - **Refinement**: 确立了窗口式滑动预计算（3屏缓冲）与固定字阶 Bitmap Font 方案。

### [2026-04-12] 知识花园体系化
- **事件**: 正式激活 `DEBT_LEADGER` 与 `CHRONICLE`。
- **意义**: 标志着项目进入无人值守的自主管理阶段，所有技术债与决策历史实现闭环追踪。

### [2026-04-09] 核心模型映射 (ADR-002)
- **事件**: 确立了将 `sys_dept` 深度语义化为 Family 的架构决策。
- **影响**: 后端实现逻辑彻底从“组织管理”转向“家庭协同”，为多端数据隔离奠定基础。
- **关联**: [[ADR-002-Family-Dept-Mapping]]

### [2026-04-08] 环境准备与初期配置
- 初始化项目工程，整合 `ruoyi` 后端与移动端 App 框架。
- 确认了 ESP32-S3 硬件终端的交互限制 (1.8寸屏, 4按键)。
