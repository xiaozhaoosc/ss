# 2026-05-05 日志 (System Hardening & Config Audit)

## [Morning_Briefing]
- **核心目标**：基于《硬编码审计报告》启动系统加固，并确保 E2E 测试流水线的长期稳定性。
- **关键点**：重点解决配置文件中的明文密码与前端硬编码的 `localhost` 引用。

## 执行摘要 (Execution Summary)

| 模块 | 动作 | 成果 |
2: | :--- | :--- | :--- |
3: | **全局审计** | 硬编码扫描 | 完成了对 `smallsteps-ui`、`api`、`app` 及 `esp32` 源码的全面审计，识别出 4 大类硬编码风险。 |
4: | **测试流水线** | E2E 稳定性加固 | 永久禁用了测试环境下的 `Intro.js` 引导动画，消除了 UI 自动化的主要干扰源。 |
5: | **AI 助手** | 家长模式闭环 | 验证了流式 AI 在家长端的情感诊断功能，相关决策记录于 [[ADR-014-Parent-AI-Emotional-Assistant]]。 |

## 技术细节与决策
- **配置标准化**：识别出 `application-dev.yml` 与 `vite.config.ts` 中存在大量硬编码 IP，下一步计划引入 `.env` 与 Spring Profiles 进行解耦。
- **自动化测试**：在 Playwright 层面通过环境变量 `VITE_DISABLE_GUIDE=true` 实现了对引导系统的静默控制。

## 隐患预警 (DEBT_LEADGER)
- [ ] **Secret 泄露风险**：`application-dev.yml` 中的数据库与 Redis 密码为明文，需迁移至环境变量或安全网关。
- [ ] **多语言缺失**：`smallsteps-ui` 存在超过 300 处硬编码中文文本，阻碍了国际化扩展。

## 今日建议 (Next Steps)
1. **实施配置解耦**：优先清理前端代码中的 `localhost` 引用，改用统一的 `import.meta.env.VITE_APP_BASE_API`。
2. **硬件配网优化**：将 ESP32 固件中的 WiFi SSID/PASS 提取至 `nvs` 存储，避免固件泄露导致的网络风险。

---
[[_index_wiki]]
