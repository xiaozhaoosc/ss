# 项目日志 - 2026-04-09

## [Morning_Briefing]
- **核心目标**：完成 child 与 parent 模块三端落地，实现 `sys_dept` 到 Family 的概念映射。
- **技术债务**：`smallsteps-child` 模块部分源码缺失，导致 `smallsteps-parent` 编译失败。

## 今日进展 (Family Reconstruction)
| 任务阶段 | 动作 | 结果 |
| :--- | :--- | :--- |
| **Research** | 识别 `sys_dept` 与 Family 的映射潜力 | 输出 [[ADR-002-Family-Dept-Mapping]] |
| **API 实施** | 扩展 `ParentTask`、`ChildTask` 实体，增加 `deptId` | 数据库字段与实体同步完成 |
| **API 实施** | 新增 `ParentFamilyController` 实现家庭成员绑定 | 逻辑落地，支持 `members` 查询与 `bind-child` |
| **App 实施** | 新增 `family.ts` 接口定义 | 已支持 `getFamilyMembers` 与 `bindChild` |
| **自愈修复** | 重建 `smallsteps-child` 中缺失的 `ChildAI` 系列服务 | 成功补齐 Mock 版本，扫清跨模块依赖死锁 |

## 待办与隐患 (INBOX)
- [ ] 解决 `smallsteps-parent` 编译时对 `com.alibaba.excel` 的依赖解析问题。
- [ ] 实现 `smallsteps-ui` 中的家庭管理可视化界面。
- [ ] 编写全自动 API 测试脚本验证 `dept_id` 隔离逻辑。

## 影子观察者记录 (Shadow Observer)
- **Bad Smell**: `smallsteps-parent` 模块中部分 Vo 的导入包路径与其父 POM 定义的依赖版本不一致。
- **建议**: 统一各模块对 `BaseEntity` 的引用路径，目前存在 `common-core` 与 `common-mybatis` 两个冲突的存疑路径。已优先采用 `common-mybatis` 路径。
