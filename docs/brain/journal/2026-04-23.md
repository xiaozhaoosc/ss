# 2026-04-23: AI 模块探索与架构沉淀 [[2026-04-23]]

## [Morning_Briefing]
- **当前状态**: 项目已从核心业务开发转向文档化与深度优化阶段。今日重点是整理 `smallsteps-ai` 模块的架构逻辑，并解决遗留的技术债。
- **今日目标**: 
    - [x] 完成 AI 模块架构文档沉淀。
    - [ ] 修复 `AiModelController` 中的 404 隐患。
    - [ ] 验证 `smallsteps-admin` 对 AI 模块的依赖加载。

## 关键进展
### 1. 架构梳理 (AI Module)
完成了 [[SmallSteps-AI-Architecture]] 的编写。确认了模块采用 MyBatis-Plus + Hutool BeanUtil 的组合，避开了 Lombok 在多模块环境下的编译问题。

### 2. 知识花园更新
- 更新了 [[_index_wiki]]，增加了 AI 架构的索引。
- 在 [[INBOX]] 中记录了关于 RESTful 路径参数的安全建议。

## 风险与阻塞
- **Lombok 兼容性**: 跨模块继承 `BaseEntity` 时仍需警惕 `getter/setter` 丢失，建议维持显式转换或使用原生 Java 记录类（如果版本支持）。
- **网络波动**: 之前记录的 Playwright 内核下载问题仍需在执行测试前解决。

## 灵感碎片
- 既然 AI 模块已经有了 `AiPrompt`，可以考虑增加一个“Prompt 版本控制”功能，允许在不重启服务的情况下热切换线上 Prompt 策略。

---
*影子观察者: 发现 `smallsteps-ai` 的配置文件中仍有明文 API Key 的占位符，建议在生产环境强制使用环境变量注入。*
