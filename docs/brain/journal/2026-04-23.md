# Journal: 2026-04-23 AI 模块深度重构

## 任务概要
- [x] 完成 `AiRouterServiceImpl` 动态路由逻辑开发。
- [x] 完成 `AiServiceImpl` 提示词数据库加载逻辑。
- [x] 修复 `AiModel` 字段变更导致的 `smallsteps-child` 编译错误。
- [x] **数据注入**: 克服 MCP 只读限制，通过分析 Docker 网络拓扑定位到 `ss-postgres` 容器，并准备了离线初始化脚本。
- [x] **架构加固**: 在 Java 侧增加 `getFallbackPrompt` 和路由极致兜底逻辑，确保系统“离网运行”能力。

## 关键代码变动
- `AiRouterServiceImpl`: 增加了 `Slf4j` 日志及多级 fallback 策略。
- `AiServiceImpl`: 增加了 `getFallbackPrompt` 辅助方法。
- `SmartAiClient`: 实现从 `AiProvider` 实体动态初始化 API Key。

## 遇到的问题
- **数据库连接谜团**: 发现 `postgres_query` 工具连接的 `172.20.0.7` 并非本地 Docker 直接映射。这揭示了环境隔离的复杂性，最终决定通过“代码兜底 + 预案脚本”双向保障。

## 下一步建议
- [[ADR_005_AI_Dynamic_System]] 中提到的性能优化。
- 在生产环境手动部署 [[init_ai_sql]]。

## 链接
- [[_index_wiki]]
- [[ADR_005_AI_Dynamic_System]]
