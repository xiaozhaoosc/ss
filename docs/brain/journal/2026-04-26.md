# 2026-04-26 技术日志 (Small Steps)

## [Morning_Briefing]
- **核心焦点**：Web 全自动化回归测试、修复路由混乱、补全 AI 配置字段。
- **状态现状**：昨天重构了 AI 后端，但前端路由出现分裂（`ai/` vs `system/ai/`），导致侧边栏部分失效。

## 关键发现 (Automated Testing)
- **路由异常**: 发现系统存在两套 AI 管理视图目录。当前生效的 `ai/model` 路径与 `system/ai/` 预设不符，导致 404。
- **UI 缺失**: 模型管理对话框缺失 `context_window` 字段。
- **登录状态**: admin 登录正常，Dashboard 数据渲染正常。

## 关键发现 (Parent Insight Module Discovery)
- **数据链路**: `ss_task` (ParentTask) 定义层 -> `ss_task_log` (ChildTask) 执行层 -> `ChildTaskVo` 视图聚合。
- **看板接口聚合**: `ParentInsightController` 为家长看板入口，聚合了从 `IParentTaskService` (任务与总结报表)、`IChildAIService` (情绪记录与分析)、`IScoreService` (积分历史) 三方的结果。
- **技术债 (Mock 数据)**: `getWeeklyReport` 与 `getAbilityRadarByChildId` 内部存在硬编码的伪数据（如最近 7 天情绪 `List.of(5, 4, 5, 3, 4, 5, 4)`），需要对接到真实的记录。

## [Daily_Summary]
| 目标 | 状态 | 成果 |
| :--- | :--- | :--- |
| Web 自动化测试 | [x] | 完成 `http://localhost:88/` 核心链路测试，记录 4 项 Bug |
| Bug 修复 (App) | [x] | 修复刷新导致状态丢失、后端儿童权限校验、任务创建者 ID 注入问题 |
| AI 聊天体验优化 | [x] | 攻克 SSE 流式响应被代理截断或超时问题，实现“秒开”反馈 |
| Bug 记录 | [x] | 交付 `bug_web_20260426.md` 与 `bug_20260426.md` |
| 家长数据洞察探索 | [x] | 理清 `ParentInsightController` 数据流，记录于 [[Parent-Insight-Module-Architecture]] |
| 火山引擎模型调研 | [x] | 完成豆包大模型接入路径调研，编写 [[volcengine_integration]] 指南 |

## 关键技术动作 (Evening Session)
- **状态恢复**: 在 `App.vue` 的 `onLaunch` 中注入主动重登录逻辑，解决 H5 环境刷新页面后 Vuex/Pinia 状态清空导致的“未登录”假象。
- **权限加固**: 重写 `validateChildAccess` 逻辑，允许儿童本人访问其个人任务/成就/AI 记录，解除之前因全局 `SaIgnore` 导致的越权隐患，并修复了 5 个核心业务 Controller 的权限拦截。
- **SSE 流控**: 通过后端发送“预热空格块”解决了 Spring `SseEmitter` 的长等待挂起问题，沉淀 [[ADR-010-AI-Streaming-SSE-Optimization]]。

## 今日建议
- **重构建议**: 彻底清理 `src/views/ai`，统一迁移至 `src/views/system/ai`。
- **测试建议**: 针对 UI 回归测试，建议开始引入 **Playwright** 脚本（参考 `TEST_SPEC.md`），替代手动点选。
- **消灭 Mock 数据**: 优先替换 `ParentInsightController` 中月报、周报、能力雷达图内部写死的魔法值，对齐数据库真实的业务计算结果。
